class Solution:
    def segregateElements(self, arr):
        
        neg=[]
        pos=[]
        for i in arr:
            if i>=0:
                pos.append(i)
            else:
                neg.append(i)
        result=pos+neg
        for i in range(len(arr)):
            arr[i]=result[i]
      
