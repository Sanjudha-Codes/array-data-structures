class Solution:
    def findDuplicates(self, arr):
        res=[]
        for i in range(0,len(arr)):
            for j in range(i+1,len(arr)):
                if arr[i]==arr[j]:
                    res.append(arr[i])
                    
        return res      
