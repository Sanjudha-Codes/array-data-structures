class Solution:
    def isPalindrome(self, s):
        # code here
        pal = list(s)
        
        left,right=0, len(pal)-1
        
        while left<=right:
            
            if pal[left]!=pal[right]:
                return False
            left+=1
            right-=1
        return True 
            
