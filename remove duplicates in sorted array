class Solution:
    def removeDuplicates(self, arr):
        n=len(arr)
        if n==0:
            return 0
        ans=[]
        for i in range(0,n-1):
            if arr[i]!=arr[i+1]:
                ans.append(arr[i])
                
        ans.append(arr[n-1])        
        return ans
