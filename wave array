class Solution:
    def sortInWave(self, arr):
        n=len(arr)
        if n<2:
            return arr
            
        for i in range(n-1):
            if i%2==0:
                arr[i],arr[i+1]=arr[i+1],arr[i]
        return arr    
